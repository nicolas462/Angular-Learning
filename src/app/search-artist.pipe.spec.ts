import { SearchArtistPipe } from './search-artist.pipe';

describe('SearchArtistPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchArtistPipe();
    expect(pipe).toBeTruthy();
  });
});
